version: "1.0.0"
development: Yes

services:
  service.device-registry:
    package: service.device-registry
    name: "Device Registry"
    port: 4000
  service.config:
    package: service.config
    name: "Cofig Service"
    port: 4001
  service.controller.plug:
    package: service.controller.plug
    name: "Tasmota Plug Controller"
    port: 4002
